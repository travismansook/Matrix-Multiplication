# Matrix-Multiplication

•	Wrote a C program to multiply two square matrices read from binary files, with dimension and bounds validation
•	Implemented binary file I/O (fread/fwrite) to read matrix data and serialize the multiplication result back to disk
